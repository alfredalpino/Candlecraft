# Archived legacy scripts

These standalone scripts predate the `candlecraft` package (v0.1.x) and are **no longer maintained**.

Use instead:

- **Library:** `from candlecraft import fetch_ohlcv`
- **CLI:** `python pull_ohlcv.py --help`

Files kept here for historical reference only.
